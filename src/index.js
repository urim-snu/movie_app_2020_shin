import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

//component, it shows data. funcion that returns HTML.
//JSX, HTML in javascript
ReactDOM.render(<App />, document.getElementById("root"));
